import displayComponents from "@/lib/utils/displayComponents";

const Components = ({ componentsData, componentsList }) => {
  return displayComponents(componentsData, componentsList);
};

export default Components;
